import { Typography } from "@material-ui/core";
const Home = () => {
  return <Typography
    variant="h4"
    component="h2"
    className="navBody">
    HOME

  </Typography>;
};

export default Home;