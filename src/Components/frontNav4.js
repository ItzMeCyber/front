import { Typography } from "@material-ui/core";
const Contact = () => {
  return <Typography variant="h4"
    color="Primary"
    component="h2">
    CONTACT

  </Typography>;;
};

export default Contact;